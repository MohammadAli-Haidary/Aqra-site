# 🔐 سیستم ثبت‌نام ادمین

## ✅ ویژگی‌ها

سیستم ثبت‌نام ادمین با قابلیت‌های کامل:

- ✅ ثبت‌نام ادمین جدید
- ✅ ورود ادمین موجود
- ✅ اعتبارسنجی کامل (نام کاربری، ایمیل، رمز عبور)
- ✅ ذخیره‌سازی امن در MongoDB
- ✅ رمزگذاری رمز عبور با bcrypt
- ✅ تولید توکن JWT خودکار
- ✅ ورود خودکار پس از ثبت‌نام

---

## 📋 فیلدهای ثبت‌نام

| فیلد | توضیح | الزامی | اعتبارسنجی |
|------|--------|---------|-------------|
| **نام کامل** | نام و نام خانوادگی | ❌ خیر | - |
| **نام کاربری** | شناسه ورود | ✅ بله | حداقل 3 کاراکتر، یکتا |
| **ایمیل** | آدرس ایمیل | ✅ بله | فرمت ایمیل، یکتا |
| **رمز عبور** | رمز ورود | ✅ بله | حداقل 6 کاراکتر |
| **تکرار رمز عبور** | تأیید رمز | ✅ بله | مطابقت با رمز عبور |

---

## 🚀 نحوه استفاده

### 1. دسترسی به صفحه ثبت‌نام

1. سایت را باز کنید
2. روی دکمه **⚙️** (پایین سمت راست) کلیک کنید
3. روی لینک **"ثبت‌نام کنید"** کلیک کنید

### 2. ثبت‌نام ادمین جدید

```
1. نام کامل خود را وارد کنید (اختیاری)
2. نام کاربری انتخاب کنید (حداقل 3 کاراکتر)
3. ایمیل خود را وارد کنید
4. رمز عبور قوی انتخاب کنید (حداقل 6 کاراکتر)
5. رمز عبور را تکرار کنید
6. روی "ثبت‌نام" کلیک کنید
```

### 3. ورود خودکار

پس از ثبت‌نام موفق:
- ✅ توکن JWT تولید می‌شود
- ✅ به صورت خودکار وارد می‌شوید
- ✅ به پنل مدیریت هدایت می‌شوید

---

## 🔒 امنیت

### رمزگذاری رمز عبور

```javascript
// در Backend (server/models/Admin.js)
adminSchema.pre('save', async function(next) {
  if (!this.isModified('password')) return next();
  this.password = await bcrypt.hash(this.password, 10);
  next();
});
```

### اعتبارسنجی

**Backend:**
- ✅ بررسی فیلدهای الزامی
- ✅ بررسی طول نام کاربری (حداقل 3)
- ✅ بررسی طول رمز عبور (حداقل 6)
- ✅ بررسی یکتا بودن نام کاربری
- ✅ بررسی یکتا بودن ایمیل

**Frontend:**
- ✅ اعتبارسنجی فرم HTML5
- ✅ بررسی مطابقت رمز عبور
- ✅ نمایش خطاهای اعتبارسنجی

---

## 📊 ساختار دیتابیس

### Collection: `admins`

```javascript
{
  _id: ObjectId,
  username: "admin",           // یکتا
  password: "$2a$10$...",      // bcrypt hash
  email: "admin@egra-book.af", // یکتا
  full_name: "مدیر سایت",
  last_login: Date,
  createdAt: Date,
  updatedAt: Date
}
```

---

## 🔧 API Endpoints

### ثبت‌نام

```http
POST /api/auth/register
Content-Type: application/json

{
  "username": "newadmin",
  "password": "securepass123",
  "email": "newadmin@egra-book.af",
  "full_name: "نام کامل"
}
```

**پاسخ موفق:**
```json
{
  "status": "success",
  "message": "ثبت‌نام با موفقیت انجام شد",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "admin": {
      "id": "65f1234567890abcdef12345",
      "username": "newadmin",
      "email": "newadmin@egra-book.af",
      "full_name": "نام کامل"
    }
  }
}
```

**پاسخ خطا:**
```json
{
  "status": "error",
  "message": "این نام کاربری قبلاً ثبت شده است"
}
```

---

## 🎨 رابط کاربری

### صفحه ثبت‌نام

```
┌─────────────────────────────────────┐
│                                     │
│        🧑‍💼                         │
│   ثبت‌نام ادمین جدید                │
│   کتاب‌فروشی اقرأ                   │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 👤 نام کامل                   │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🏷️ نام کاربری *              │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ ✉️ ایمیل *                   │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🔒 رمز عبور *                │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🔒 تکرار رمز عبور *          │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │      ثبت‌نام                   │ │
│  └───────────────────────────────┘ │
│                                     │
│  قبلاً ثبت‌نام کرده‌اید؟           │
│  وارد شوید                         │
│                                     │
└─────────────────────────────────────┘
```

---

## 🔄 سوییچ بین ورود و ثبت‌نام

### از ورود به ثبت‌نام:
```
صفحه ورود → لینک "ثبت‌نام کنید" → صفحه ثبت‌نام
```

### از ثبت‌نام به ورود:
```
صفحه ثبت‌نام → لینک "وارد شوید" → صفحه ورود
```

---

## 🛡️ نکات امنیتی

### برای ادمین‌ها:

1. **رمز عبور قوی:**
   - حداقل 8 کاراکتر
   - ترکیب حروف بزرگ و کوچک
   - استفاده از اعداد و نمادها
   - مثال: `Secure@Pass123`

2. **ایمیل معتبر:**
   - از ایمیل واقعی استفاده کنید
   - ایمیل باید قابل دسترسی باشد

3. **نام کاربری یکتا:**
   - نام کاربری قابل تغییر نیست
   - با دقت انتخاب کنید

### برای توسعه‌دهندگان:

1. **JWT Secret:**
   ```env
   # در .env
   JWT_SECRET=very-long-secure-random-string-at-least-32-chars
   ```

2. **Rate Limiting:**
   ```javascript
   // در server/index.js
   const limiter = rateLimit({
     windowMs: 15 * 60 * 1000, // 15 دقیقه
     max: 5 // حداکثر 5 ثبت‌نام در 15 دقیقه
   });
   app.use('/api/auth/register', limiter);
   ```

3. **Email Verification (پیشنهادی):**
   - ارسال ایمیل تأیید
   - فعال‌سازی حساب با لینک

---

## 📝 مثال‌های استفاده

### مثال 1: ثبت‌نام موفق

```javascript
// Frontend
await api.register(
  'newadmin',
  'securepass123',
  'newadmin@egra-book.af',
  'مدیر جدید'
);
// ✅ ثبت‌نام موفق
// ✅ ورود خودکار
// ✅ هدایت به پنل مدیریت
```

### مثال 2: نام کاربری تکراری

```javascript
// Frontend
try {
  await api.register('admin', 'pass123', 'email@test.com');
} catch (error) {
  console.error(error.message);
  // ❌ "این نام کاربری قبلاً ثبت شده است"
}
```

### مثال 3: رمز عبور ضعیف

```javascript
// Frontend validation
if (password.length < 6) {
  setError('رمز عبور باید حداقل 6 کاراکتر باشد');
  return;
}
// ❌ خطا نمایش داده می‌شود
```

---

## 🎯 گردش کار کامل

```
1. کاربر روی ⚙️ کلیک می‌کند
   ↓
2. صفحه ورود نمایش داده می‌شود
   ↓
3. کاربر روی "ثبت‌نام کنید" کلیک می‌کند
   ↓
4. صفحه ثبت‌نام نمایش داده می‌شود
   ↓
5. کاربر فرم را پر می‌کند
   ↓
6. اعتبارسنجی Frontend انجام می‌شود
   ↓
7. درخواست POST به /api/auth/register ارسال می‌شود
   ↓
8. Backend اعتبارسنجی می‌کند
   ↓
9. رمز عبور با bcrypt hash می‌شود
   ↓
10. ادمین در MongoDB ذخیره می‌شود
   ↓
11. توکن JWT تولید می‌شود
   ↓
12. توکن در localStorage ذخیره می‌شود
   ↓
13. کاربر به پنل مدیریت هدایت می‌شود
```

---

## 🔍 عیب‌یابی

### مشکل: "این نام کاربری قبلاً ثبت شده است"

**علت:** نام کاربری تکراری است

**راه‌حل:** نام کاربری دیگری انتخاب کنید

---

### مشکل: "این ایمیل قبلاً ثبت شده است"

**علت:** ایمیل تکراری است

**راه‌حل:** از ایمیل دیگری استفاده کنید

---

### مشکل: "رمز عبور و تکرار آن مطابقت ندارند"

**علت:** رمز عبور و تکرار آن متفاوت هستند

**راه‌حل:** هر دو فیلد را یکسان پر کنید

---

### مشکل: "خطا در ثبت‌نام"

**علت:** Backend در حال اجرا نیست

**راه‌حل:**
```bash
cd server
npm run dev
```

---

## 📊 آمار

### تعداد ادمین‌ها

```javascript
// در MongoDB
db.admins.countDocuments()
```

### مشاهده همه ادمین‌ها

```javascript
// در MongoDB Compass
db.admins.find({}, { password: 0 })
```

---

## 🎓 نکات تکمیلی

### افزودن نقش‌ها (Roles)

```javascript
// در server/models/Admin.js
const adminSchema = new mongoose.Schema({
  // ... فیلدهای موجود
  role: {
    type: String,
    enum: ['super_admin', 'admin', 'editor'],
    default: 'admin'
  }
});
```

### محدود کردن ثبت‌نام

```javascript
// فقط ادمین اصلی می‌تواند ادمین جدید ثبت‌نام کند
router.post('/register', authMiddleware, async (req, res) => {
  // بررسی نقش ادمین فعلی
  if (req.admin.role !== 'super_admin') {
    return res.status(403).json({
      status: 'error',
      message: 'فقط ادمین اصلی می‌تواند ادمین جدید ثبت‌نام کند'
    });
  }
  // ... ادامه کد
});
```

---

## ✅ چک‌لیست

- [ ] Backend در حال اجرا است
- [ ] MongoDB متصل است
- [ ] صفحه ثبت‌نام قابل دسترسی است
- [ ] اعتبارسنجی فرم کار می‌کند
- [ ] ثبت‌نام موفق انجام می‌شود
- [ ] ورود خودکار پس از ثبت‌نام کار می‌کند
- [ ] توکن JWT در localStorage ذخیره می‌شود
- [ ] پنل مدیریت قابل دسترسی است

---

## 📞 پشتیبانی

اگر مشکلی دارید:
- 📧 info@egra-book.af
- 📱 +93 70 123 4567

---

**سیستم ثبت‌نام ادمین کامل و آماده استفاده است! 🎉**
